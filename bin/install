#!/bin/bash -e

source $OPENSHIFT_CARTRIDGE_SDK_BASH
version="$1"

shopt -s dotglob

mkdir -p $OPENSHIFT_PUMA_DIR/template
cp -r $OPENSHIFT_PUMA_DIR/versions/shared/template/* $OPENSHIFT_PUMA_DIR/template
